import { AppShell } from "@/components/layout/app-shell";
import { LeadSourcesWorkspace } from "@/components/leads/lead-sources-workspace";

export default function LeadSourcesPage() {
  return (
    <AppShell>
      <LeadSourcesWorkspace />
    </AppShell>
  );
}
